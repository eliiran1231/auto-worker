# Used by the reviewer worker.
REVIEWER_GITHUB_TOKEN=
# Used by the coder worker and orchestrator (lookups and merges).
CODER_GITHUB_TOKEN=
# Used by the tester worker and workflow dispatches.
TESTER_GITHUB_TOKEN=
WEBHOOK_SECRET=
