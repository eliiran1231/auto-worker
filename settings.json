{
  "server": {
    "port": 3000,
    "webhookPath": "/webhook"
  },
  "github": {
    "username": "eliiran1231",
    "linkedIssuesLimit": 10
  },
  "workspace": {
    "defaultRoot": "c:/",
    "repositoryPrefixLength": 3,
    "issueDirectoryTemplate": "./{repositoryPrefix}-i-{issueNumber}",
    "testerDirectoryTemplate": "./{repositoryPrefix}-tests"
  },
  "agents": {
    "coderType": "codex",
    "reviewerType": "claude",
    "testerType": "codex"
  },
  "prompts": {
    "workOnIssue": "work on issue {issueUrl} and open a PR assigned to me.",
    "reviewPullRequest": "review this pull request {pullRequestUrl}. if you approve, submit approval. if you don't approve, submit changes requested with your feedback. make sure to run the differential test before approving",
    "reReviewPullRequest": "your review got a response on {pullRequestUrl}. re-review the pull request and submit your feedback. make sure to run the differential test before approving.",
    "addressReview": "you got a review on {pullRequestUrl}",
    "findBugs": "inspect this repository for reproducible compatibility gapes between this library and microsoft's original directoryServices. verify each finding and create a GitHub issue for the confirmed gaps",
    "writeTests": "inspect this repository and add focused tests for important untested behavior and likely regressions",
    "analyzeTestResultsAndCreateIssues": "analyze the completed test workflow at {testResultUrl}, and create a GitHub assigned to me issue for your findings."
  },
  "queries": {
    "linkedIssues": "query($owner: String!, $repo: String!, $pr: Int!, $limit: Int!) { repository(owner: $owner, name: $repo) { pullRequest(number: $pr) { closingIssuesReferences(first: $limit) { nodes { id number title } } } } }"
  }
}
